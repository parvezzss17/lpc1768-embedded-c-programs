project 3:
Blinking all the LEDS one at time
#include <lpc17xx.h>

int main(){
    LPC_GPIO1->FIODIR = (0xff<<19);

    while(1){
        int i = 0;
        LPC_GPIO1->FIOCLR = (0xff<<19);
		LPC_GPIO1->FIOSET = (1<<19);
		for(i=0; i<10000; i++);
	
        LPC_GPIO1->FIOCLR = (0xff<<19);
		LPC_GPIO1->FIOSET = (1<<20);
		for(i=0; i<10000; i++);
	
		LPC_GPIO1->FIOCLR = (0xff<<19);
		LPC_GPIO1->FIOSET = (1<<21);
		for(i=0; i<50000; i++);

		LPC_GPIO1->FIOCLR = (0xff<<19);
		LPC_GPIO1->FIOSET = (1<<22);
		for(i=0; i<50000; i++);

		LPC_GPIO1->FIOCLR = (0xff<<19);
		LPC_GPIO1->FIOSET = (1<<23);
		for(i=0; i<50000; i++);

		LPC_GPIO1->FIOCLR = (0xff<<19);
		LPC_GPIO1->FIOSET = (1<<24);
		for(i=0; i<50000; i++);

		LPC_GPIO1->FIOCLR = (0xff<<19);
		LPC_GPIO1->FIOSET = (1<<25);
		for(i=0; i<50000; i++);

		LPC_GPIO1->FIOCLR = (0xff<<19);
		LPC_GPIO1->FIOSET = (1<<26);
		for(i=0; i<50000; i++);
	
 
    }
}
